import './Menu.scss'
import { useState } from 'react'

const Menu = (props) => {
    return (
        <div className={`menu ${props.page}`}>
            <p>Menu placeholder</p>
        </div>
    )
}

export default Menu