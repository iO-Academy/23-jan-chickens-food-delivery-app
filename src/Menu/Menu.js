import './Menu.scss'

const Menu = (props) => {
    return (
        <div className={`menu ${props.displayRestaurantsOrMenu}`}>
            <p>Menu placeholder</p>
        </div>
    )
}

export default Menu